components:
  teacher_encoder:
    backend: external
    factory: your_wan_adapter:create_encoder
    checkpoint: D:/weights/Wan2.1_VAE.pth
    kwargs: {}
    freeze: true
    adapter: {kind: encoder, input_mode: rgb_video, temporal_frames: 1}
  teacher_decoder:
    backend: external
    factory: your_wan_adapter:create_decoder
    checkpoint: D:/weights/Wan2.1_VAE.pth
    kwargs: {}
    freeze: true
    adapter: {kind: decoder, output_mode: rgb}

