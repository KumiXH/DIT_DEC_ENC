# Preserve vendored source bytes so manifest SHA256 values remain stable on Windows.
third_party/** -text whitespace=-trailing-space,-blank-at-eof
src/distill_codec/vendor/** -text whitespace=-trailing-space,-blank-at-eof
